# gradle-local.properties
# ⚠ 此文件不会被提交（已加入 .gitignore）
# 用于存放本地机器特有的 Gradle 配置，比如 JDK 路径。
#
# 使用方式：取消下面一行的注释并改为你的 JDK 17 路径。
# org.gradle.java.home=D:/workspace/gradle/jdks/jdk-17.0.2

# 你也可以在这里添加其他本地覆盖，例如：
# systemProp.file.encoding=UTF-8
